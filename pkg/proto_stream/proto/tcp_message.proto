syntax = "proto3";

package proto;
option go_package = "pkg/proto_stream/pb";

message TCPMessage {
  string type = 1; // `ping`, `tcp_handshake`, `message`, `encrypted_message`, etc.
  bytes payload = 2;
}
